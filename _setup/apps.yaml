apps:
  # CLI
  - fastfetch
  - fzf
  - ripgrep
  - starship

  # Runtimes
  - { brew: node, apt: nodejs, pacman: nodejs }
  - { brew: pnpm, pacman: pnpm }
  - { brew: go, apt: golang, pacman: go }
  - { brew: python, apt: python3, pacman: python }
  - { brew: openjdk, apt: default-jdk, pacman: jdk-openjdk }
  - { brew: dotnet-sdk, apt: dotnet-sdk-8.0, pacman: dotnet-sdk }

  # Editor/terminal
  - { brew: neovim, apt: neovim, pacman: neovim }
  - { brew: wezterm }

  # Desktop / WM (macOS)
  - { brew: raycast }
  - { brew: nikitabobko/tap/aerospace }

  # Docker (simple)
  - { brew: docker, apt: docker.io, pacman: docker }

  # Fonts (macOS via cask-fonts)
  - { brew: font-recursive-mono-nerd-font }
  - { brew: font-jetbrains-mono-nerd-font }
  - { brew: font-fira-code-nerd-font }
  - { brew: font-symbols-only-nerd-font }
