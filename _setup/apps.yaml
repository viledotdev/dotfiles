apps:
  cli:
    - fastfetch
    - fzf

  runtimes:
    - { name: node, brew: node, apt: nodejs, pacman: nodejs }
    - { name: npm, brew: npm, apt: npm, pacman: npm }
    - { name: pnpm, brew: pnpm, apt: pnpm, pacman: pnpm }
    - { name: dotnet, brew: dotnet, apt: dotnet-sdk-8.0, pacman: dotnet-sdk }
    - { name: java, brew: openjdk, apt: default-jdk, pacman: jdk-openjdk }
    - { name: go, brew: go, apt: golang, pacman: go }
    - { name: python, brew: python, apt: python3, pacman: python }

  dev:
    - { name: docker, brew: docker, apt: docker.io, pacman: docker }
    - {
        name: docker-compose,
        brew: docker-compose,
        apt: docker-compose,
        pacman: docker-compose,
      }
