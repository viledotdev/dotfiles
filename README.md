# Dotfiles Setup (zsh + yq)

## Requisitos

- **zsh** as shell
- **sudo** available (Linux)
- **brew** installed (Homebrew (MacOS))
- **yq** installed for YAML on CLI
- **git** installed

### Install yq and git

```
#MacOS
brew install yq git
```

### Install config

```
./setup.sh
```
